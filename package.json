{
  "name": "gen-format",
  "version": "1.0.0",
  "description": "General formatting system; string, number, date, currency, (expandable)",
  "main": "build/src/Formatter.js",
  "scripts": {
    "test": "npm run clean && npm run compile && tap ./build/test/*.test.js",
    "test-direct": "npm run clean && npm run compile && node build/test/Localized.test.js",
    "clean": "rm -fr build css",
    "compile": "tsc",
    "doc": "npm run clean && npm run compile && inchjs && cd build; tsccd ./; documentation lint src; documentation readme --readme-file ../API.md --markdown-toc false --section API src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^15.12.4",
    "inchjs": "^0.4.1",
    "tap": "^14.11.0",
    "ts-clear-classdoc": "^1.0.1"
  },
  "dependencies": {
    "@tremho/locale-string-tables": "^0.5.0",
    "fs-extra": "^10.0.0",
    "moment-timezone": "^0.5.33",
    "timezone-support": "^2.0.2"
  }
}
